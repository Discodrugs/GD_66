variable is a container that has something
or becomes a substitute for something extensive for example.

toggle buttons
one button can do both actions instead of seperate buttons

console first shows status and then runs a check